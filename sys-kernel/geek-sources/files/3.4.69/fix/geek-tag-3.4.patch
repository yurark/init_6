diff -x .git -urN linux-3.4/scripts/mkcompile_h geek/scripts/mkcompile_h
--- linux-3.4/scripts/mkcompile_h	2012-05-21 01:29:13.000000000 +0300
+++ geek/scripts/mkcompile_h	2013-11-04 20:06:10.091662823 +0200
@@ -54,8 +54,8 @@
 fi
 
 UTS_VERSION="#$VERSION"
-CONFIG_FLAGS=""
-if [ -n "$SMP" ] ; then CONFIG_FLAGS="SMP"; fi
+CONFIG_FLAGS="GEEK"
+if [ -n "$SMP" ] ; then CONFIG_FLAGS="$CONFIG_FLAGS SMP"; fi
 if [ -n "$PREEMPT" ] ; then CONFIG_FLAGS="$CONFIG_FLAGS PREEMPT"; fi
 UTS_VERSION="$UTS_VERSION $CONFIG_FLAGS $TIMESTAMP"
 
